gsap.config({
    nullTargetWarn: !1
});
var M = {
    data: null,
    init: function() {
        M.loadData()
    },
    loadData: function() {
        var a = new XMLHttpRequest;
        a.onreadystatechange = function() {
            if (200 === a.status && 4 === a.readyState) {
                var d = JSON.parse(a.responseText);
                M.data = d
            }
        }
        ;
        a.open("post", "./data.json");
        a.send()
    }
}
var V = {
    hero: null,
    projects: null,
    main: null,
    init: function() {
        V.loader()
    },
    loader: function() {
        CustomEase.create("easeOutExpo", "0.16, 1, 0.3, 1");
        CustomEase.create("easeInExpo", "0.7, 0, 0.84, 0");
        CustomEase.create("strongEaseInOut", "0.08, 0.82, 0.17, 1");
        CustomEase.create("strongEaseInLinearOut", "0.08, 0.82, 1, 1");
        var a = {
            value: 0
        };
        gsap.timeline({}).to(".loader-line", {
            ease: "easeOutExpo",
            duration: 4.5, //change the duration of loading screen 2
        },).to(".black-div", {
            translateY: "-90px",
            ease: "strongEaseInOut",
            duration: 2,
        }, 0.5).to(".black-div-two", {
            translateY: "-90px",
            ease: "strongEaseInOut",
            duration: 2,
        }, 3.5).to(".loader", {
            opacity: 0,
            ease: "easeOutExpo",
            onComplete: function() {
                document.querySelector(".loader").remove()
            }
        }, 4).to([".logo", ".availability"], {
            opacity: 1,
            ease: "easeInExpo"
        }, 4).to(".hero-line", {
            x: 0,
            y: 0,
            z: 0,
            ease: "easeOutExpo",
            duration: 1, //time before animation starts1
            stagger: {
                each: 0.1 //time for each line 0.1
            }
        }, 4)
    }
}
var App = {
    init: function() {
        M.init();
        V.init()
    }
};
window.addEventListener("load", function() {
    App.init()
});

document.addEventListener("DOMContentLoaded", function() {
    CustomEase.create("easeOutExpo", "0.16, 1, 0.3, 1");
    CustomEase.create("easeInExpo", "0.7, 0, 0.84, 0");

    var touchTimelines = {};

    function createTimelines(cardId) {

        var projClick = document.getElementById(cardId + "-click");
        var projImg = document.getElementById(cardId + "-img");
        var tlProjEnter = gsap.timeline({ paused: true });
        var tlProjLeave = gsap.timeline({ paused: true });
        var tlProjEnterTouch = gsap.timeline({ paused: true });
        var tlProjLeaveTouch = gsap.timeline({ paused: true });
        var tlProjLeavePage = gsap.timeline({ paused: true });
        var currentIndex = cardIDs.indexOf(cardId);
        var cardElements = cardIDs.filter(function(id, index) {
            return Math.abs(currentIndex - index) > 0;
        }).map(function(id) {
            return document.getElementById(id + "-click");
        });
        touchTimelines[cardId] = { enter: tlProjEnterTouch, leave: tlProjLeaveTouch };

        console.log("HERE MAN" + cardElements);
        
        tlProjLeavePage.to(["body", ".default", ".pointer", "#" + cardId + "-img:hover"], {
            cursor: "none",
        }).to([".logo", ".availability"], {
            opacity: 0,
            ease: "easeInExpo"
        }).to(".hero-line", {
            x: 0,
            y: 2000,
            z: 0,
            ease: "easeInExpo",
            duration: 1,
            stagger: {
                each: 0.1
            }
        }).to({}, {
            duration: 0,
            onComplete: function() {
                window.location.href = cardId + ".html";
            }
        });

        tlProjEnter.to("." + cardId + "-line", {
            x: 0,
            y: 0,
            z: 0,
            ease: "easeOutExpo",
            duration: 0.5,
            stagger: {
                each: 0.1
            }
        }).to({}, {
            duration: 0,
            onComplete: function() {
                projImg.style.transitionDelay = "0.5s";
            }
        })

        tlProjLeave.to("." + cardId + "-line", {
            x: 0,
            y: 2000,
            z: 0,
            ease: "easeInExpo",
            duration: 0.5,
            stagger: {
                each: 0.1
            }
        }).to({}, {
            duration: 0,
            onComplete: function() {
                projImg.style.transitionDelay = "";
            }
        });

        tlProjEnterTouch.to("#" + cardId + "-img", {
            opacity: 0.2,
            duration: 0.5
        }, 0.3).to("." + cardId + "-line", {
            x: 0,
            y: 0,
            z: 0,
            ease: "easeOutExpo",
            duration: 0.5,
            stagger: {
                each: 0.1
            }
        }).to({}, {
            duration: 0,
            onComplete: function() {
                projImg.style.transitionDelay = "0.5s";
            }
        });

        tlProjLeaveTouch.to("." + cardId + "-line", {
            x: 0,
            y: 2000,
            z: 0,
            ease: "easeInExpo",
            duration: 0.5,
            stagger: {
                each: 0.1
            }
        }, 0.1).to("#" + cardId + "-img", {
            opacity: 1,
            duration: 0.5
        }).to({}, {
            duration: 0,
            onComplete: function() {
                projImg.style.transitionDelay = "";
            }
        });

        projClick.addEventListener("mouseenter", function() {
            tlProjEnter.restart()
        });

        projClick.addEventListener("mouseleave", function() {
            tlProjLeave.restart()
        });

        projClick.addEventListener("click", function() {
            function scrollToTop() {
                window.scrollTo({
                    top: 0,
                    behavior: "smooth"
                });
            }
            projImg.setAttribute("id", cardId + "-img-post")
            document.body.style.overflow = "hidden";
            scrollToTop();
            setTimeout(function() {
                tlProjLeavePage.restart();
            },0);
        })

        projClick.addEventListener("touchstart", function() {
            // for (var id in touchTimelines) {
            //     if (id !== cardId) {
            //         touchTimelines[id].leave.pause();
            //     }
            // }
            // setTimeout(function() {
                // tlProjLeaveTouch.pause();
                tlProjEnterTouch.restart();
                setTimeout(function() {
                    createTimelines(cardId)
                },0.5)
            // })
            
        });

        function touchStartHandler() {
            tlProjLeaveTouch.restart();
            setTimeout(function() {
            cardElements.forEach(function(card) {
              card.removeEventListener("touchstart", touchStartHandler);
            });
                createTimelines(cardID);
            },0.5)
          }
          
        cardElements.forEach(function(card) {
            card.addEventListener("touchstart", touchStartHandler);
          });

    }

    const cardIDs = ['proja', 'projb', 'projc', 'projd', 'proje', 'projf', 'projg', 'projh', 'proji', 'projj', 'projk', 'projl'];
    cardIDs.forEach(function(cardID) {
        createTimelines(cardID);
    });
})
